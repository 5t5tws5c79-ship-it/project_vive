[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# 지금은 라우터가 없어 불필요하지만, 나중에 vue-router를 붙이면
# 새로고침 시 404가 나는 걸 막아준다.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
